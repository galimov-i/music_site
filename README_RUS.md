# Сайт Музыканта Ильшата Галимова

Современный одностраничный лендинг для музыкального артиста Ильшата Галимова на Flask.

![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)
![Flask](https://img.shields.io/badge/Flask-3.0+-green.svg)
![SQLite](https://img.shields.io/badge/SQLite-3-lightgrey.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

[🇬🇧 English Version](README.md)

## Возможности

- 🎵 **Интеграция с музыкальными площадками** - VK Музыка и Яндекс Музыка
- 💰 **Система донатов** - Boosty, DonationAlerts, VK Donut, криптокошельки
- 📝 **Форма заказа песни** - С тарифами и описанием
- 🎓 **Продажа курса** - Курс по продакшену с оплатой через ЮКассу
- 📱 **Адаптивный дизайн** - Mobile-first подход
- ✨ **Современные анимации** - Плавный скролл и fade-in эффекты
- 📧 **Email-уведомления** - Интеграция с Flask-Mail
- 🔒 **Безопасность** - Rate limiting, защита от XSS, заголовки безопасности

## Технологии

| Компонент | Технология |
|-----------|------------|
| Backend | Flask 3.0+ (Python) |
| База данных | SQLite |
| Frontend | HTML5, CSS3, Vanilla JavaScript |
| Платежи | ЮКасса |
| Email | Flask-Mail |
| Шрифты | Google Fonts (Montserrat, Open Sans) |

## Быстрый старт

### Требования

- Python 3.10+
- pip (менеджер пакетов Python)

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd ilshat-galimov-landing

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

# Копирование файла окружения
cp .env.example .env
# Отредактируйте .env своими значениями

# Запуск приложения
python app.py
```

Откройте http://localhost:5000 в браузере.

## Структура проекта

```
├── app.py                  # Фабрика Flask приложения
├── config.py               # Настройки конфигурации
├── requirements.txt        # Python зависимости
├── .env.example            # Шаблон переменных окружения
├── database/
│   ├── __init__.py
│   ├── db.py               # Подключение к БД
│   └── schema.sql          # SQL схема
├── routes/
│   ├── __init__.py
│   ├── main.py             # Основные маршруты
│   ├── forms.py            # Обработчики форм (с rate limiting)
│   └── payment.py          # Интеграция платежей
├── models/
│   ├── __init__.py
│   ├── song_order.py       # Модель заказа песни
│   ├── course_purchase.py  # Модель покупки курса
│   └── contact.py          # Модель контакта
├── static/
│   ├── css/
│   │   ├── style.css       # Основные стили (~1500 строк)
│   │   ├── animations.css  # Стили анимаций
│   │   └── responsive.css  # Медиа-запросы
│   ├── js/
│   │   ├── main.js         # Основной JavaScript
│   │   ├── animations.js   # Scroll-анимации
│   │   ├── forms.js        # Валидация форм
│   │   └── payment.js      # Обработка платежей
│   └── images/
│       ├── favicon.svg
│       └── og-image.svg
└── templates/
    ├── base.html           # Базовый шаблон
    ├── index.html          # Лендинг
    └── email/              # Email шаблоны
```

## Настройка

### Переменные окружения

| Переменная | Обязательно | Описание |
|------------|-------------|----------|
| `SECRET_KEY` | Да | Секретный ключ Flask (генерация: `python -c "import secrets; print(secrets.token_hex(32))"`) |
| `YOOKASSA_SHOP_ID` | Нет | ID магазина ЮКассы |
| `YOOKASSA_SECRET_KEY` | Нет | Секретный ключ ЮКассы |
| `MAIL_SERVER` | Нет | SMTP сервер (по умолчанию: smtp.yandex.ru) |
| `MAIL_PORT` | Нет | SMTP порт (по умолчанию: 587) |
| `MAIL_USERNAME` | Нет | Email адрес |
| `MAIL_PASSWORD` | Нет | Пароль приложения |
| `ADMIN_EMAIL` | Нет | Email для уведомлений |

### Настройка платежей ЮКасса

1. Зарегистрируйтесь на [yookassa.ru](https://yookassa.ru/)
2. Создайте магазин и получите данные
3. Добавьте в `.env`:
   ```
   YOOKASSA_SHOP_ID=ваш_shop_id
   YOOKASSA_SECRET_KEY=ваш_secret_key
   ```
4. Настройте вебхук: `https://ваш-домен.ru/webhook/yookassa`
5. Протестируйте в тестовом режиме

## API Эндпоинты

| Метод | Эндпоинт | Описание | Rate Limit |
|-------|----------|----------|------------|
| GET | `/` | Главная страница | Нет |
| POST | `/submit-song-order` | Заказ песни | Да (5/мин) |
| POST | `/submit-contact` | Форма контактов | Да (5/мин) |
| POST | `/subscribe-newsletter` | Подписка | Да (5/мин) |
| POST | `/create-payment` | Оплата курса | Нет |
| GET | `/payment-success` | Успешная оплата | Нет |
| GET | `/payment-failure` | Неуспешная оплата | Нет |
| POST | `/webhook/yookassa` | Вебхук оплаты | Нет |

## Схема базы данных

### Таблицы

| Таблица | Описание |
|---------|----------|
| `song_orders` | Заявки на заказ песен |
| `course_purchases` | Записи о покупках курса |
| `contact_submissions` | Сообщения из формы контактов |
| `newsletter_subscribers` | Подписчики рассылки |

### Инициализация БД

```bash
# Через Flask CLI
flask init-db

# Или программно
python -c "from app import app; from database.db import init_db; app.app_context().push(); init_db()"
```

## Кастомизация

### Цветовая схема

Редактируйте CSS переменные в `static/css/style.css`:

```css
:root {
    --color-primary: #0077FF;      /* VK синий */
    --color-secondary: #FFCC00;    /* Яндекс жёлтый */
    --color-accent: #FF3347;       /* Акцентный красный */
    --color-bg-dark: #0A0A0F;      /* Фон */
    --color-text-primary: #FFFFFF; /* Текст */
}
```

### Контент

Редактируйте контент в `templates/index.html`:
- Hero секция (строки 3-30)
- Секция "Обо мне" (строки 35-75)
- Детали курса (строки 100-180)
- Тарифы (строки 220-280)

### Добавление изображений

Замените плейсхолдеры в `static/images/`:
- `hero/` - Фон Hero секции
- `about/` - Фотографии артиста
- `course/` - Превью курса

## Развёртывание

### Docker

```dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .
EXPOSE 5000

CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:5000", "app:app"]
```

```bash
docker build -t ilshat-music .
docker run -p 5000:5000 --env-file .env ilshat-music
```

### Gunicorn (Продакшен)

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

### PythonAnywhere

1. Загрузите файлы через Web интерфейс или Git
2. Создайте виртуальное окружение
3. Установите зависимости: `pip install -r requirements.txt`
4. Настройте WSGI файл для импорта `app` из `app.py`
5. Установите переменные окружения во вкладке Web
6. Перезагрузите веб-приложение

### Nginx Reverse Proxy

```nginx
server {
    listen 80;
    server_name ilshatgalimov.ru;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /static {
        alias /path/to/app/static;
        expires 30d;
    }
}
```

## Функции безопасности

- **Rate Limiting** - 5 запросов в минуту на IP для форм
- **Защита от XSS** - Экранирование пользовательского ввода в emails
- **Заголовки безопасности** - X-Content-Type-Options, X-Frame-Options и др.
- **Защита от SQL-инъекций** - Параметризованные запросы
- **CSRF Ready** - Можно добавить Flask-WTF для форм

## Производительность

| Метрика | Цель |
|---------|------|
| Lighthouse Score | 90+ |
| First Contentful Paint | < 1.5с |
| Time to Interactive | < 3с |
| Размер бандла | < 100KB (CSS+JS) |

### Советы по оптимизации

1. Включите gzip сжатие в Nginx
2. Используйте CDN для статики
3. Добавьте `loading="lazy"` к изображениям ниже первого экрана
4. Минифицируйте CSS/JS в продакшене

## Поддержка браузеров

| Браузер | Версия |
|---------|--------|
| Chrome | 90+ |
| Firefox | 88+ |
| Safari | 14+ |
| Edge | 90+ |
| Яндекс Браузер | Последняя |

## Решение проблем

### Проблемы с БД

```bash
# Сброс базы данных
rm music_site.db
python -c "from app import app; from database.db import init_db; app.app_context().push(); init_db()"
```

### Email не отправляется

1. Проверьте переменные MAIL_* в окружении
2. Для Яндекс Почты создайте пароль приложения
3. Проверьте папку спам
4. Убедитесь в порте SMTP (587 для TLS)

### Платежи не работают

1. Проверьте данные ЮКассы
2. Убедитесь, что вебхук URL доступен
3. Сначала тестируйте в песочнице
4. Проверьте логи Flask на ошибки

## Разработка

### Запуск тестов

```bash
python -m pytest tests/
```

### Стиль кода

```bash
# Установка линтеров
pip install flake8 black

# Проверка кода
flake8 .
black --check .
```

### Добавление новых функций

1. Создайте ветку для фичи
2. Добавьте тесты при необходимости
3. Обновите документацию
4. Отправьте pull request

## Секции лендинга

| # | Секция | Описание |
|---|--------|----------|
| 1 | Hero | Полноэкранное приветствие с CTA |
| 2 | Обо мне | Биография и статистика |
| 3 | Музыка | Ссылки на VK и Яндекс Музыку |
| 4 | Курс | Курс по продакшену с ценой 4990₽ |
| 5 | Заказ песни | Тарифы: 5000₽ / 10000₽ / 20000₽ |
| 6 | Поддержка | Ссылки на донаты |
| 7 | Контакты | Форма связи |
| 8 | Футер | Навигация и соцсети |

## Лицензия

MIT License - см. [LICENSE](LICENSE) для деталей.

## Контакты

- **Telegram**: [@ilshatgalimov](https://t.me/ilshator)
- **VK**: [vk.com/ilshatgalimov](https://vk.com/ilshatgalimov)
- **Email**: ilshat.music@example.com

---

Сделано с ❤️ для Ильшата Галимова | © 2026
